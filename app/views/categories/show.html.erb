<h1> All Projects under the <%= @category.category %> category </h1>

<h2> <% @category.projects.each do |project| %>
    <p> <%= link_to project.title, project %> </p>
    <p> <%= project.description %> </p>
    <% end %>
