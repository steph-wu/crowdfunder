<!DOCTYPE html>
<html>
  <head>
    <title>Crowdfunder</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://use.fontawesome.com/8004be3262.js"></script>
    <%= stylesheet_link_tag    "https://fonts.googleapis.com/css?family=Open+Sans:300,600|Roboto:300,600" %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="main">
    <header class="site-header">
      <h1 class="logo"><i class="fa fa-diamond" aria-hidden="true"></i> <%= link_to "crowdfunder.", root_path %></h1>

      <nav class="site-nav">
        <% if current_user %>
          <%= link_to "Profile", current_user %>
          <%= link_to "Logout", :logout, method: :post %>
        <% else %>
          <%= link_to "Register", new_user_path %>
          <%= link_to "Login", :login %>
        <% end %>

      </nav>
    </header>

    <% flash.each do |key, message| %>
      <div class="flash-<%= key %>"><%= message %></div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <footer class="site-footer">
      <h1 class="footer-logo">crowdfunder.</h1>
    </footer>
  </div>
  </body>
</html>
